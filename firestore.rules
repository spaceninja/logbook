rules_version = '2';

// PRODUCTION rules. Items are world-readable; writes are blocked entirely until
// the auth milestone replaces `false` with an owner-uid check (core design §7).
service cloud.firestore {
  match /databases/{database}/documents {
    match /items/{id} {
      allow read: if true;
      allow write: if false;
    }
  }
}
